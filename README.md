## PowerBI Thingamajigs

Some scripts for stuffs that I believe PowerBI Desktop should have done better, but they didn't so I resorted to Python.

Example commands:

```bash
uv sync

uv run prune_pbi.py ../PowerBI_project_dir/ --run
```